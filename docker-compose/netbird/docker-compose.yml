# Helix Stax — Netbird Self-Hosted VPN
# Version: v0.66.4
# Network: helix-net (shared with postgres, redis, authentik, harbor, openbao)
# Data: /data/netbird/
#
# Ports exposed:
#   10000/tcp  — Signal server (must reach peers)
#   33073/tcp  — Management API (internal, behind proxy later)
#   33081/tcp  — Dashboard HTTP (moved from 33080 to avoid relay collision)
#   33080/tcp  — Relay server
#   3478/udp   — Coturn STUN/TURN
#   49152-65535/udp — Coturn relay range
#
# Identity Provider: Authentik (OIDC via https://auth.helixstax.net)
# Dashboard auth vars use placeholders — set NETBIRD_CLIENT_ID in .env at deploy time.

x-default: &default
  restart: unless-stopped
  logging:
    driver: json-file
    options:
      max-size: "100m"
      max-file: "3"

networks:
  helix-net:
    external: true

volumes:
  netbird-mgmt:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /data/netbird/management
  netbird-signal:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /data/netbird/signal

services:

  # ── Dashboard ─────────────────────────────────────────────────────────────
  netbird-dashboard:
    <<: *default
    image: netbirdio/dashboard:latest
    container_name: netbird-dashboard
    networks:
      - helix-net
    ports:
      # Changed from 33080:80 to 33081:80 — port 33080 is used by netbird-relay
      # (relay must keep 33080 because it's referenced in management.json Relay.Addresses
      # and NB_EXPOSED_ADDRESS). Dashboard is behind Nginx so host port doesn't matter.
      - "${NETBIRD_IP:-127.0.0.1}:33081:80"
    environment:
      # Management API endpoint (what the browser talks to)
      - NETBIRD_MGMT_API_ENDPOINT=https://vpn.helixstax.net
      - NETBIRD_MGMT_GRPC_API_ENDPOINT=https://vpn.helixstax.net
      # Auth — Authentik OIDC (client ID set in .env at deploy time)
      - AUTH_AUDIENCE=NETBIRD_CLIENT_ID
      - AUTH_CLIENT_ID=NETBIRD_CLIENT_ID
      - AUTH_CLIENT_SECRET=
      - AUTH_AUTHORITY=https://auth.helixstax.net/application/o/netbird/
      - USE_AUTH0=false
      - AUTH_SUPPORTED_SCOPES=openid profile email offline_access api
      - AUTH_REDIRECT_URI=https://vpn.helixstax.net/auth
      - AUTH_SILENT_REDIRECT_URI=https://vpn.helixstax.net/silent-auth
      - NETBIRD_TOKEN_SOURCE=accessToken
      # No TLS on dashboard — Traefik handles it later
      - NGINX_SSL_PORT=443
      - LETSENCRYPT_DOMAIN=
      - LETSENCRYPT_EMAIL=
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ── Signal Server ─────────────────────────────────────────────────────────
  netbird-signal:
    <<: *default
    image: netbirdio/signal:latest
    container_name: netbird-signal
    networks:
      - helix-net
    ports:
      - "10000:80"
    volumes:
      - netbird-signal:/var/lib/netbird
    command:
      - "--log-file=console"
      - "--log-level=info"
      - "--port=80"
    healthcheck:
      test: ["CMD", "/usr/local/bin/netbird-signal", "health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ── Management Server ──────────────────────────────────────────────────────
  netbird-management:
    <<: *default
    image: netbirdio/management:latest
    container_name: netbird-management
    networks:
      - helix-net
    depends_on:
      netbird-dashboard:
        condition: service_healthy
    ports:
      - "33073:33073"
    volumes:
      - netbird-mgmt:/var/lib/netbird
      - ./management.json:/etc/netbird/management.json:ro
    environment:
      # PostgreSQL backend (shared helix-postgres)
      - NETBIRD_STORE_ENGINE_POSTGRES_DSN=host=helix-postgres port=5432 user=postgres password=${POSTGRES_PASSWORD} dbname=netbird sslmode=disable
    command:
      - "--port=33073"
      - "--log-file=console"
      - "--log-level=info"
      - "--disable-anonymous-metrics=true"
      - "--single-account-mode-domain=netbird.helixstax.net"
      - "--dns-domain=netbird.helixstax.net"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:33073/api/accounts"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  # ── Relay Server ──────────────────────────────────────────────────────────
  netbird-relay:
    <<: *default
    image: netbirdio/relay:latest
    container_name: netbird-relay
    networks:
      - helix-net
    ports:
      - "33080:33080"
    environment:
      - NB_LOG_LEVEL=info
      - NB_LISTEN_ADDRESS=:33080
      - NB_EXPOSED_ADDRESS=rel://5.78.145.30:33080
      - NB_AUTH_SECRET=${NETBIRD_RELAY_SECRET}

  # ── Coturn STUN/TURN ──────────────────────────────────────────────────────
  netbird-coturn:
    <<: *default
    image: coturn/coturn:latest
    container_name: netbird-coturn
    # Host network required: coturn needs to bind UDP directly for relay
    network_mode: host
    volumes:
      - ./turnserver.conf:/etc/turnserver.conf:ro
    command:
      - "-c"
      - "/etc/turnserver.conf"
