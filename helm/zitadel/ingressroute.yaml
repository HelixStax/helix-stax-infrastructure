apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: zitadel
  namespace: production
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`zitadel.helixstax.net`)
      kind: Rule
      services:
        - name: zitadel
          port: 8080
          scheme: h2c
